/* =========================================================
   config.js — credenciales de Supabase

   Reemplaza los dos valores de abajo con los de tu proyecto:
   Project Settings → API → Project URL / anon public key.

   Mientras dejes los valores de ejemplo tal cual están, la app
   sigue funcionando 100% en local (localStorage) sin nube.
   ========================================================= */

var CONFIG = {
  SUPABASE_URL: "https://TU-PROYECTO.supabase.co",
  SUPABASE_ANON_KEY: "TU-ANON-KEY-AQUI"
};
